# macOS
.DS_Store

# Editor
.vscode/*
!.vscode/settings.json
.idea/

# Env / secrets — never commit these
.env
.env.*
*.pem
*.key

# Raw source photos — large, unoptimized; converted WebP output in images/ is what ships
raw/*
!raw/.gitkeep
